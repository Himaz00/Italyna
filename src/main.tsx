// Made by Ayham Zedan
import { createRoot } from 'react-dom/client'
import App from './App.tsx'
import './index.css'

// Entry point: Mounts the React app to the root DOM node.
createRoot(document.getElementById("root")!).render(<App />);
